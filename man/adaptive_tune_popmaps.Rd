% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_popmaps.R
\name{adaptive_tune_popmaps}
\alias{adaptive_tune_popmaps}
\title{Adaptively tune POPMAPS parameters}
\usage{
adaptive_tune_popmaps(
  input_raster = "",
  input_locs = "",
  surface = "G",
  empirical_pt_dist = NULL,
  num_sites = NULL,
  num_tested = NULL,
  popmod = NULL,
  n_initial = 50,
  n_refine = 50,
  method = c("latin_hypercube", "random"),
  seed = NULL,
  threshold = 0,
  primary_metric = c("rmse", "mae", "hellinger", "dominant_accuracy",
    "dominant_probability"),
  dist_prob_func = function(popmod_temp, distance) {     exp(popmod_temp * distance) },
  quiet = TRUE
)
}
\arguments{
\item{input_raster}{A \code{terra::SpatRaster}, \code{raster::RasterLayer}, or path to
a raster file defining the interpolation surface.}

\item{input_locs}{A data frame or matrix with sampling location name,
longitude, latitude, and one or more ancestry coefficient columns.}

\item{surface}{Character. Currently only \code{"G"} is supported.}

\item{empirical_pt_dist}{Optional numeric vector or range for the rarefaction
distance search space. If \code{NULL}, values are suggested from site distances.}

\item{num_sites}{Optional integer candidate site-pool sizes.}

\item{num_tested}{Optional integer candidate numbers of sites used in each
prediction.}

\item{popmod}{Optional numeric vector or range for the distance-decay search
space. If \code{NULL}, values are suggested from site distances.}

\item{n_initial}{Number of initial parameter combinations to sample.}

\item{n_refine}{Number of additional combinations to sample around the best
initial region.}

\item{method}{Search method. \code{"latin_hypercube"} stratifies samples across
each parameter range; \code{"random"} samples independently.}

\item{seed}{Optional random seed for reproducibility.}

\item{threshold}{Numeric scalar. Raster values below this threshold are not
scored.}

\item{primary_metric}{Metric used to select the best parameter combination.}

\item{dist_prob_func}{Function defining the relationship between distance and
empirical-site contribution.}

\item{quiet}{Logical. If \code{FALSE}, print a short completion message.}
}
\value{
A \code{popmaps_adaptive_tuning} object. It has the same \code{results},
\code{folds}, and \code{best} elements as \code{tune_popmaps()}, plus a \code{search} element
describing the sampled search.
}
\description{
\code{adaptive_tune_popmaps()} samples parameter combinations from a
data-adaptive search space, evaluates them with the same leave-one-site-out
scoring used by \code{tune_popmaps()}, then optionally refines sampling around
the best-performing region.
}
\examples{
ex_raster <- raster::aggregate(hija_raster, fact = 240)
adaptive <- adaptive_tune_popmaps(
  input_raster = ex_raster,
  input_locs = hija_struc,
  n_initial = 6,
  n_refine = 4,
  seed = 1,
  quiet = TRUE
)
adaptive$best

}
