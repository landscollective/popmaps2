% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-converters.R
\name{surface_from_eems}
\alias{surface_from_eems}
\title{Convert EEMS output to a POPMAPS conductance surface}
\usage{
surface_from_eems(
  input,
  value_col = NULL,
  coordinate_cols = NULL,
  template = NULL,
  resolution = NULL,
  buffer = NULL,
  target_cells = 100,
  crs = "",
  surface_values = c("conductance", "resistance", "suitability"),
  rescale_conductance = FALSE,
  resistance_epsilon = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{input}{A raster-like object/path, data frame, or matrix. Tabular inputs
are rasterized using \code{template} or a grid derived from their coordinates.}

\item{value_col}{Surface-value column for tabular inputs. If \code{NULL}, common
names such as \code{conductance}, \code{migration}, \code{value}, \code{mean}, \code{m}, or \code{w} are
detected.}

\item{coordinate_cols}{Optional coordinate column names or integer positions
for data frame or matrix inputs. If \code{NULL}, common coordinate names are
detected first; otherwise POPMAPS-style columns 2 and 3 are used when the
first column is non-numeric, and columns 1 and 2 are used as a final
fallback.}

\item{template}{Optional raster defining output geometry for tabular inputs.}

\item{resolution}{Raster cell size in coordinate units. If \code{NULL}, a
resolution is chosen so the longest coordinate span has about
\code{target_cells} cells.}

\item{buffer}{Extra distance added around the coordinate bounding box. If
\code{NULL}, the larger of one cell or five percent of the longest coordinate
span is used.}

\item{target_cells}{Approximate number of cells along the longest coordinate
axis when \code{resolution = NULL}.}

\item{crs}{Coordinate reference system assigned to the created raster. The
default \code{""} leaves the CRS unset; pass a CRS string when the generated
grid should carry projection metadata.}

\item{surface_values}{Character. Meaning of raster values when
\code{surface = "C"}. \code{"suitability"} and \code{"conductance"} use values directly;
\code{"resistance"} converts values to conductance using an inverse transform.
Ignored when \code{surface = "G"}.}

\item{rescale_conductance}{Logical. If \code{TRUE}, rescale non-missing
conductance values to the range 0-1 after any resistance conversion. The
default is \code{FALSE} to preserve legacy POPMAPS behavior for MaxEnt logistic
suitability rasters.}

\item{resistance_epsilon}{Positive numeric scalar added to resistance values
before inversion to avoid infinite conductance when resistance is zero.}
}
\value{
A \code{popmaps_surface} object.
}
\description{
\code{surface_from_eems()} converts raster-like EEMS outputs or coordinate/value
tables to a \code{surface = "C"} POPMAPS surface. Tables must contain x/y
coordinates plus one numeric surface-value column. High migration values
should usually be treated as conductance; effective resistance-style outputs
can be passed with \code{surface_values = "resistance"}.
}
\examples{
eems_table <- expand.grid(x = 1:3, y = 1:3)
eems_table$migration <- seq(0.2, 1, length.out = nrow(eems_table))
eems_surface <- surface_from_eems(
  eems_table,
  value_col = "migration",
  resolution = 1
)
eems_surface

}
