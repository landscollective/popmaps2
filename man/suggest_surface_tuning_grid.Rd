% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_popmaps.R
\name{suggest_surface_tuning_grid}
\alias{suggest_surface_tuning_grid}
\title{Suggest a POPMAPS tuning grid from a candidate surface}
\usage{
suggest_surface_tuning_grid(
  input_raster,
  input_locs,
  surface = "G",
  surface_values = c("suitability", "conductance", "resistance"),
  num_sites = NULL,
  num_tested = NULL,
  empirical_pt_dist_probs = c(0.05, 0.1, 0.25),
  distance_weights = c(0.95, 0.75, 0.5, 0.25, 0.1, 0.05),
  distance_reference = c("median", "mean"),
  max_num_tested = 8,
  rescale_conductance = FALSE,
  resistance_epsilon = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{input_raster}{A \code{terra::SpatRaster}, \code{raster::RasterLayer}, or path to
a raster file defining the interpolation surface. It may also be a
\code{popmaps_surface} object returned by \code{\link[=prepare_popmaps_surface]{prepare_popmaps_surface()}}.}

\item{input_locs}{A data frame or matrix with sampling location name,
longitude, latitude, and one or more ancestry coefficient columns.}

\item{surface}{Character. \code{"G"} tunes geographic-distance interpolation.
\code{"C"} tunes suitability- or conductance-weighted least-cost interpolation
using the modern internal distance helper.}

\item{surface_values}{Character. Meaning of raster values when
\code{surface = "C"}. \code{"suitability"} and \code{"conductance"} use values directly;
\code{"resistance"} converts values to conductance before distances are
calculated. Ignored when \code{input_raster} is already a \code{popmaps_surface}
object.}

\item{num_sites}{Integer vector. Candidate pool sizes to evaluate.}

\item{num_tested}{Integer vector. Numbers of empirical sites used to estimate
ancestry coefficients.}

\item{empirical_pt_dist_probs}{Distance quantiles used to suggest
\code{empirical_pt_dist}, after always including zero.}

\item{distance_weights}{Desired weights retained at the reference distance;
converted to \code{popmod} values with \code{log(weight) / reference_distance}.}

\item{distance_reference}{Which site-distance summary to use as the
reference distance for \code{popmod}.}

\item{max_num_tested}{Maximum automatically suggested \code{num_tested} value.}

\item{rescale_conductance}{Logical. If \code{TRUE}, rescale conductance values to
0-1 before least-cost distances are calculated for \code{surface = "C"}.}

\item{resistance_epsilon}{Positive numeric scalar added to resistance values
before inversion when \code{surface_values = "resistance"}.}
}
\value{
A \code{popmaps_tuning_grid} list.
}
\description{
\code{suggest_surface_tuning_grid()} is the surface-aware companion to
\code{\link[=suggest_tuning_grid]{suggest_tuning_grid()}}. It derives \code{empirical_pt_dist} and \code{popmod} from
empirical sampling-site distances measured over the selected surface. For
\code{surface = "G"} those distances are geographic kilometers. For
\code{surface = "C"} they are least-cost distances through a suitability,
conductance, or resistance surface.
}
\examples{
ex_raster <- terra::rast(raster::aggregate(hija_raster, fact = 240))
grid <- suggest_surface_tuning_grid(
  input_raster = ex_raster,
  input_locs = hija_struc,
  surface = "G"
)
grid$distance_units

}
