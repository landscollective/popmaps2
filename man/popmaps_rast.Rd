% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popmaps-output.R
\name{popmaps_rast}
\alias{popmaps_rast}
\title{Convert POPMAPS output to a terra raster}
\usage{
popmaps_rast(pop_raster_list, input_raster, layer_names = NULL)
}
\arguments{
\item{pop_raster_list}{A list returned by \code{\link[=popmaps]{popmaps()}}.}

\item{input_raster}{The raster used to create \code{pop_raster_list}. It may be a
\code{terra::SpatRaster}, a legacy \code{raster::RasterLayer}, or a path readable by
\code{terra::rast()}.}

\item{layer_names}{Optional character vector of layer names. By default,
layers are named \code{hard_boundary}, \code{ancestry_probability}, \code{axis_1},
\code{axis_2}, and so on.}
}
\value{
A \code{terra::SpatRaster} with one layer per \code{popmaps()} output matrix.
}
\description{
Converts the list returned by \code{\link[=popmaps]{popmaps()}} into a named
\code{terra::SpatRaster} using the extent, resolution, and coordinate reference
system from the original input raster.
}
\examples{
ex_raster <- raster::aggregate(hija_raster, fact = 240)
aps <- popmaps(
  input_raster = ex_raster,
  input_locs = hija_struc,
  surface = "G",
  empirical_pt_dist = 0,
  num_sites = 5,
  num_tested = 2,
  popmod = -0.05,
  threshold = 0,
  ncore = 1
)
popmaps_rast(aps, ex_raster)

}
