% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popmaps-output.R
\name{write_popmaps}
\alias{write_popmaps}
\title{Write POPMAPS output layers as GeoTIFFs}
\usage{
write_popmaps(
  pop_raster_list,
  input_raster,
  dir = ".",
  prefix = "popmaps",
  layer_names = NULL,
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{pop_raster_list}{A list returned by \code{popmaps()}.}

\item{input_raster}{The raster used to create \code{pop_raster_list}.}

\item{dir}{Output directory for GeoTIFF files. Created if it does not exist.}

\item{prefix}{File name prefix used for each layer.}

\item{layer_names}{Optional character vector of layer names passed to
\code{\link[=popmaps_rast]{popmaps_rast()}}.}

\item{overwrite}{Logical. If \code{FALSE}, the function stops before writing when
any output file already exists.}

\item{...}{Additional arguments passed to \code{terra::writeRaster()}.}
}
\value{
A data frame with \code{layer} and \code{path} columns.
}
\description{
Writes each layer returned by \code{\link[=popmaps_rast]{popmaps_rast()}} to a separate
GeoTIFF. The function returns a small manifest containing the layer names and
written file paths.
}
\examples{
\dontrun{
paths <- write_popmaps(aps, ex_raster, dir = "outputs", prefix = "hija")
}
}
