% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popmaps-viz.R
\name{plot_popmaps}
\alias{plot_popmaps}
\title{Plot modern POPMAPS raster outputs}
\usage{
plot_popmaps(
  pop_raster_list,
  input_raster,
  input_locs = NULL,
  type = c("confidence", "boundary", "axis", "ancestry"),
  style = c("modern", "manuscript"),
  axis = 1,
  sites = c("pies", "points", "none"),
  site_legend = TRUE,
  boundaries = TRUE,
  palette = "Viridis",
  boundary_palette = "Dark 3",
  background_raster = NULL,
  background_threshold = NULL,
  background_palette = "manuscript_background",
  state_lines = FALSE,
  state_col = "grey15",
  state_lwd = 0.8,
  n = 100,
  legend = TRUE,
  axes = TRUE,
  frame.plot = FALSE,
  main = NULL,
  col_na = "transparent",
  boundary_alpha = 0.2,
  boundary_lwd = 1.4,
  pie_radius = NULL,
  point_cex = 1,
  add = FALSE,
  ...
)
}
\arguments{
\item{pop_raster_list}{A list returned by \code{\link[=popmaps]{popmaps()}}.}

\item{input_raster}{The raster used to create \code{pop_raster_list}. It may be a
\code{terra::SpatRaster}, a legacy \code{raster::RasterLayer}, or a raster file path.}

\item{input_locs}{Optional POPMAPS location table. When supplied, empirical
sampling locations are drawn as ancestry pies or dominant-ancestry points.}

\item{type}{Map type to draw. \code{"confidence"} plots the dominant ancestry
confidence layer, \code{"boundary"} plots hard population assignments, and
\code{"axis"} plots one weighted ancestry-axis layer. \code{"ancestry"} is accepted
as a legacy alias for \code{"confidence"}.}

\item{style}{Plot style. \code{"modern"} uses the default popmaps2 map style.
\code{"manuscript"} uses a Massatti and Winkler (2022)-inspired style with
grayscale confidence, colored hard-boundary outlines, muted suitability
background, and state outlines when available.}

\item{axis}{Ancestry axis used when \code{type = "axis"}. Supply either an integer
axis number or a layer name such as \code{"axis_1"}.}

\item{sites}{How to draw empirical locations when \code{input_locs} is supplied.
\code{"pies"} draws ancestry-proportion pies, \code{"points"} draws points colored by
dominant ancestry, and \code{"none"} suppresses site symbols.}

\item{site_legend}{Logical. If \code{TRUE}, draw an ancestry-axis legend for site
symbols.}

\item{boundaries}{Logical. If \code{TRUE}, hard-boundary outlines are overlaid on
ancestry and axis maps.}

\item{palette}{Continuous color palette for ancestry and axis maps. Named
palettes are matched to base R HCL palettes; common choices include
\code{"Viridis"}, \code{"Mako"}, \code{"Inferno"}, \code{"YlGnBu"}, and \code{"Purple-Yellow"}.
A custom color vector may also be supplied.}

\item{boundary_palette}{Categorical color palette for ancestry axes and hard
boundaries. Named palettes are matched to base R HCL palettes, or a custom
color vector may be supplied.}

\item{background_raster}{Optional suitability, habitat, or prediction raster
drawn beneath the POPMAPS confidence layer. In \code{style = "manuscript"}, this
defaults to \code{input_raster}.}

\item{background_threshold}{Optional numeric cutoff. When supplied, ancestry
confidence or axis values are shown only where \code{background_raster} is
greater than or equal to the cutoff; lower cells show only the background.}

\item{background_palette}{Color palette for \code{background_raster}.}

\item{state_lines}{Logical. If \code{TRUE}, overlay state boundaries from the
\code{maps} package. This is intended for US longitude/latitude outputs.}

\item{state_col}{Color for state boundaries.}

\item{state_lwd}{Line width for state boundaries.}

\item{n}{Number of colors used for continuous raster maps.}

\item{legend}{Logical. If \code{TRUE}, draw raster and site legends.}

\item{axes}{Logical. If \code{TRUE}, draw map axes.}

\item{frame.plot}{Logical. If \code{TRUE}, draw a frame around the map panel.}

\item{main}{Optional plot title. If \code{NULL}, a title is chosen from \code{type}.}

\item{col_na}{Color used for \code{NA} raster cells.}

\item{boundary_alpha}{Fill transparency for hard-boundary maps.}

\item{boundary_lwd}{Line width for hard-boundary outlines.}

\item{pie_radius}{Radius for empirical-site pies in map units. When \code{NULL},
a conservative radius is chosen from the map extent.}

\item{point_cex}{Point size for \code{sites = "points"}.}

\item{add}{Logical. If \code{TRUE}, add the raster layer to the existing plot.}

\item{...}{Additional arguments passed to \code{terra::plot()}.}
}
\value{
Invisibly returns the \code{terra::SpatRaster} created by \code{\link[=popmaps_rast]{popmaps_rast()}}.
}
\description{
\code{plot_popmaps()} draws cleaner, \code{terra}-based maps from the list returned by
\code{\link[=popmaps]{popmaps()}}. It is intended as the modern plotting layer for popmaps2, while
\code{\link[=popmap_viz]{popmap_viz()}} remains available for POPMAPS 1.03 compatibility.
}
\examples{
ex_raster <- raster::aggregate(hija_raster, fact = 240)
aps <- popmaps(
  input_raster = ex_raster,
  input_locs = hija_struc,
  surface = "G",
  empirical_pt_dist = 0,
  num_sites = 5,
  num_tested = 2,
  popmod = -0.05,
  threshold = 0,
  ncore = 1
)
plot_popmaps(aps, ex_raster, hija_struc)

}
