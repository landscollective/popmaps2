% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-converters.R
\name{locs_from_sf}
\alias{locs_from_sf}
\title{Convert sf points to a POPMAPS location table}
\usage{
locs_from_sf(
  x,
  ancestry_cols = NULL,
  site_col = NULL,
  crs = "EPSG:4326",
  coord_names = c("lon", "lat")
)
}
\arguments{
\item{x}{An \code{sf} object with point geometries.}

\item{ancestry_cols}{Ancestry coefficient columns, supplied as names or
integer positions. If \code{NULL}, all numeric non-geometry columns except
\code{site_col} are used.}

\item{site_col}{Optional site identifier column, supplied as a name or integer
position. If \code{NULL}, a column named \code{site}, \code{sample}, \code{id}, \code{name}, or
\code{population} is used when present; otherwise synthetic site names are
created.}

\item{crs}{CRS to transform the coordinates to before extracting x/y values.
The default is \code{"EPSG:4326"} for longitude/latitude output. Set \code{crs = NULL}
to keep the input CRS.}

\item{coord_names}{Names for the returned coordinate columns.}
}
\value{
A data frame accepted by POPMAPS modeling functions.
}
\description{
\code{locs_from_sf()} converts \code{sf} point features into the data frame shape
expected by \code{popmaps()}, \code{tune_popmaps()}, and
\code{\link[=compare_popmaps_surfaces]{compare_popmaps_surfaces()}}. The returned table has site, longitude,
latitude, and ancestry coefficient columns.
}
\examples{
if (requireNamespace("sf", quietly = TRUE)) {
  locs <- locs_from_sf(sf::st_as_sf(
    data.frame(site = "a", axis1 = 1, axis2 = 0, lon = -110, lat = 39),
    coords = c("lon", "lat"),
    crs = 4326
  ))
}

}
