% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_popmaps.R
\name{diagnose_tuning}
\alias{diagnose_tuning}
\title{Diagnose a POPMAPS tuning result}
\usage{
diagnose_tuning(
  tuning,
  primary_metric = tuning$primary_metric,
  near_best_tolerance = 0.05,
  complete_only = TRUE
)
}
\arguments{
\item{tuning}{A \code{popmaps_tuning} or \code{popmaps_adaptive_tuning} object returned
by \code{tune_popmaps()} or \code{adaptive_tune_popmaps()}.}

\item{primary_metric}{Metric used to rank parameter combinations. Defaults to
the metric stored in \code{tuning}.}

\item{near_best_tolerance}{Non-negative relative tolerance used to define
near-best parameter combinations. The default, \code{0.05}, keeps combinations
within 5\% of the best score for the primary metric.}

\item{complete_only}{Logical. If \code{TRUE}, diagnose only parameter combinations
with no failed validation folds.}
}
\value{
A \code{popmaps_tuning_diagnostics} list with:
\describe{
\item{overview}{One-row summary of tuning strength and near-best support.}
\item{near_best}{Parameter combinations within \code{near_best_tolerance} of the best score.}
\item{parameter_ranges}{Near-best and full-grid support for each tuning parameter.}
\item{parameter_effects}{Average score by parameter value.}
}
}
\description{
\code{diagnose_tuning()} summarizes whether parameter tuning found a
clearly supported parameter combination or a broad set of similarly
performing alternatives. It is intended to help users interpret tuning
output biologically instead of choosing a row from \code{tuning$results} by eye.
}
\examples{
ex_raster <- raster::aggregate(hija_raster, fact = 240)
tuning <- tune_popmaps(
  input_raster = ex_raster,
  input_locs = hija_struc,
  empirical_pt_dist = c(0, 5),
  num_sites = c(5, 6),
  num_tested = c(2, 3),
  popmod = c(-0.01, -0.05),
  quiet = TRUE
)
diagnose_tuning(tuning)

}
