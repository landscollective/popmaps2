% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-converters.R
\name{surfaces_from_raster_stack}
\alias{surfaces_from_raster_stack}
\title{Create candidate surfaces from a raster stack}
\usage{
surfaces_from_raster_stack(
  input_raster,
  surface = "C",
  surface_values = "suitability",
  names = NULL,
  include_geographic = FALSE,
  geographic_name = "geographic",
  mask = NULL,
  barrier = NULL,
  rescale_conductance = FALSE,
  resistance_epsilon = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{input_raster}{A multi-layer \code{terra::SpatRaster}, legacy raster stack or
brick, or raster file path.}

\item{surface}{Character. \code{"G"} uses raster geometry only. \code{"C"} uses raster
values as a conductance-like landscape surface.}

\item{surface_values}{Character. Meaning of raster values when
\code{surface = "C"}. \code{"suitability"} and \code{"conductance"} use values directly;
\code{"resistance"} converts values to conductance using an inverse transform.
Ignored when \code{surface = "G"}.}

\item{names}{Optional candidate surface names. Defaults to raster layer names
when available.}

\item{include_geographic}{Logical. If \code{TRUE}, add a geographic surface using
the first layer's geometry.}

\item{geographic_name}{Name for the optional geographic surface.}

\item{mask}{Optional raster with the same geometry as \code{input_raster}.
Non-zero, non-\code{NA} cells mark cells eligible for prediction. This is kept
separate from conductance because a prediction mask is not necessarily a
movement barrier.}

\item{barrier}{Optional raster with the same geometry as \code{input_raster}.
Non-zero, non-\code{NA} cells mark cells that should be treated as
non-traversable in modern least-cost workflows.}

\item{rescale_conductance}{Logical. If \code{TRUE}, rescale non-missing
conductance values to the range 0-1 after any resistance conversion. The
default is \code{FALSE} to preserve legacy POPMAPS behavior for MaxEnt logistic
suitability rasters.}

\item{resistance_epsilon}{Positive numeric scalar added to resistance values
before inversion to avoid infinite conductance when resistance is zero.}
}
\value{
A named list of \code{popmaps_surface} objects.
}
\description{
\code{surfaces_from_raster_stack()} converts each layer of a multi-layer raster
into a named \code{popmaps_surface} object. This is a convenience helper for
comparing several user-supplied candidate surfaces with
\code{\link[=compare_popmaps_surfaces]{compare_popmaps_surfaces()}}.
}
