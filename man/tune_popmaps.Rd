% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_popmaps.R
\name{tune_popmaps}
\alias{tune_popmaps}
\title{Tune POPMAPS parameters with cross-validation}
\usage{
tune_popmaps(
  input_raster = "",
  input_locs = "",
  surface = "G",
  surface_values = c("suitability", "conductance", "resistance"),
  empirical_pt_dist = 5,
  num_sites = 10,
  num_tested = c(2, 3, 4, 5, 6, 7, 8),
  popmod = c(-0.001, -0.01, -0.05, -0.1, -0.15),
  threshold = 0,
  validation = c("loo", "spatial_block"),
  n_blocks = 4,
  block_assignments = NULL,
  spatial_block_repeats = 1,
  spatial_block_seed = NULL,
  primary_metric = c("rmse", "mae", "hellinger", "dominant_accuracy",
    "dominant_axis_support", "dominant_probability"),
  dist_prob_func = function(popmod_temp, distance) {
     exp(popmod_temp * distance)
 },
  rescale_conductance = FALSE,
  resistance_epsilon = sqrt(.Machine$double.eps),
  quiet = TRUE
)
}
\arguments{
\item{input_raster}{A \code{terra::SpatRaster}, \code{raster::RasterLayer}, or path to
a raster file defining the interpolation surface. It may also be a
\code{popmaps_surface} object returned by \code{\link[=prepare_popmaps_surface]{prepare_popmaps_surface()}}.}

\item{input_locs}{A data frame or matrix with sampling location name,
longitude, latitude, and one or more ancestry coefficient columns.}

\item{surface}{Character. \code{"G"} tunes geographic-distance interpolation.
\code{"C"} tunes suitability- or conductance-weighted least-cost interpolation
using the modern internal distance helper.}

\item{surface_values}{Character. Meaning of raster values when
\code{surface = "C"}. \code{"suitability"} and \code{"conductance"} use values directly;
\code{"resistance"} converts values to conductance before distances are
calculated. Ignored when \code{input_raster} is already a \code{popmaps_surface}
object.}

\item{empirical_pt_dist}{Numeric vector. Minimum distances required between
empirical sites selected for a prediction. Values are kilometers for
\code{surface = "G"} and least-cost distance units for \code{surface = "C"}.}

\item{num_sites}{Integer vector. Candidate pool sizes to evaluate.}

\item{num_tested}{Integer vector. Numbers of empirical sites used to estimate
ancestry coefficients.}

\item{popmod}{Numeric vector. Distance-decay parameter values to evaluate.}

\item{threshold}{Numeric scalar. Raster values below this threshold are not
scored.}

\item{validation}{Cross-validation design. \code{"loo"} withholds one site at a
time. \code{"spatial_block"} withholds spatially grouped sites, which is a
stricter test of prediction into undersampled regions.}

\item{n_blocks}{Target number of spatial blocks when
\code{validation = "spatial_block"} and \code{block_assignments = NULL}.}

\item{block_assignments}{Optional vector assigning each empirical site to a
spatial block. If supplied, it must have one value per row in \code{input_locs}.}

\item{spatial_block_repeats}{Number of spatial-block layouts to evaluate when
\code{validation = "spatial_block"} and \code{block_assignments = NULL}. Values
greater than one repeat the spatial-block validation with rotated spatial
partitions and report repeat-level uncertainty.}

\item{spatial_block_seed}{Optional random seed for repeated spatial-block
layouts. The first repeat uses the deterministic default partition; later
repeats use random spatial rotations.}

\item{primary_metric}{Metric used to select the best parameter combination.
\code{dominant_axis_support} is the normalized predicted support for the
observed dominant ancestry axis. \code{dominant_probability} is retained as a
legacy alias with the same values.}

\item{dist_prob_func}{Function defining the relationship between distance and
empirical-site contribution.}

\item{rescale_conductance}{Logical. If \code{TRUE}, scale conductance values by
the largest non-missing conductance value before least-cost distances are
calculated for \code{surface = "C"}.}

\item{resistance_epsilon}{Positive numeric scalar added to resistance values
before inversion when \code{surface_values = "resistance"}.}

\item{quiet}{Logical. If \code{FALSE}, print a short completion message.}
}
\value{
A \code{popmaps_tuning} object with:
\describe{
\item{results}{One row per parameter combination with average validation metrics.}
\item{folds}{One row per withheld site and parameter combination.}
\item{best}{The best row from \code{results} according to \code{primary_metric}.}
}
}
\description{
\code{tune_popmaps()} compares POPMAPS parameter combinations by
withholding empirical sampling sites, predicting ancestry coefficients from
the remaining sites, and summarizing prediction error. It supports
geographic-distance tuning and modern least-cost tuning for prepared
suitability, conductance, or resistance surfaces.
}
\examples{
ex_raster <- raster::aggregate(hija_raster, fact = 64)
tuning <- tune_popmaps(
  input_raster = ex_raster,
  input_locs = hija_struc,
  num_sites = c(8, 10),
  num_tested = c(3, 4),
  popmod = c(-0.01, -0.05),
  quiet = TRUE
)
tuning$best

}
\references{
Massatti R & Winkler DE. (2022) Spatially explicit management of
genetic diversity using ancestry probability surfaces. Methods in Ecology
and Evolution. http://dx.doi.org/10.1111/2041-210X.13902
}
