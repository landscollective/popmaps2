% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_popmaps.R
\name{suggest_tuning_grid}
\alias{suggest_tuning_grid}
\title{Suggest a data-adaptive POPMAPS tuning grid}
\usage{
suggest_tuning_grid(
  input_locs,
  num_sites = NULL,
  num_tested = NULL,
  empirical_pt_dist_probs = c(0.05, 0.1, 0.25),
  distance_weights = c(0.95, 0.75, 0.5, 0.25, 0.1, 0.05),
  distance_reference = c("median", "mean"),
  max_num_tested = 8
)
}
\arguments{
\item{input_locs}{A data frame or matrix with sampling location name,
longitude, latitude, and one or more ancestry coefficient columns.}

\item{num_sites}{Optional integer vector of candidate site-pool sizes.}

\item{num_tested}{Optional integer vector of candidate numbers of sites used
in each prediction.}

\item{empirical_pt_dist_probs}{Distance quantiles used to suggest
\code{empirical_pt_dist}, after always including zero.}

\item{distance_weights}{Desired weights retained at the reference distance;
converted to \code{popmod} values with \code{log(weight) / reference_distance}.}

\item{distance_reference}{Which site-distance summary to use as the
reference distance for \code{popmod}.}

\item{max_num_tested}{Maximum automatically suggested \code{num_tested} value.}
}
\value{
A \code{popmaps_tuning_grid} list with suggested parameter vectors and
empirical distance summaries.
}
\description{
\code{suggest_tuning_grid()} uses empirical sampling-site distances
to suggest candidate values for \code{tune_popmaps()} and
\code{adaptive_tune_popmaps()}. The suggestions are intended as a transparent
starting point, not as universal defaults.
}
\examples{
grid <- suggest_tuning_grid(hija_struc)
grid

}
