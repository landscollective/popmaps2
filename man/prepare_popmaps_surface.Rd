% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surfaces.R
\name{prepare_popmaps_surface}
\alias{prepare_popmaps_surface}
\title{Prepare a POPMAPS interpolation surface}
\usage{
prepare_popmaps_surface(
  input_raster,
  surface = c("G", "C"),
  surface_values = c("suitability", "conductance", "resistance"),
  mask = NULL,
  barrier = NULL,
  rescale_conductance = FALSE,
  resistance_epsilon = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{input_raster}{A \code{terra::SpatRaster}, legacy \code{raster::RasterLayer}, or
path to a raster file.}

\item{surface}{Character. \code{"G"} uses raster geometry only. \code{"C"} uses raster
values as a conductance-like landscape surface.}

\item{surface_values}{Character. Meaning of raster values when
\code{surface = "C"}. \code{"suitability"} and \code{"conductance"} use values directly;
\code{"resistance"} converts values to conductance using an inverse transform.
Ignored when \code{surface = "G"}.}

\item{mask}{Optional raster with the same geometry as \code{input_raster}.
Non-zero, non-\code{NA} cells mark cells eligible for prediction. This is kept
separate from conductance because a prediction mask is not necessarily a
movement barrier.}

\item{barrier}{Optional raster with the same geometry as \code{input_raster}.
Non-zero, non-\code{NA} cells mark cells that should be treated as
non-traversable in modern least-cost workflows.}

\item{rescale_conductance}{Logical. If \code{TRUE}, divide conductance by the
largest non-missing conductance value after any resistance conversion. This
preserves zero-valued barriers while putting the maximum conductance on a
0-1 scale. The default is \code{FALSE} to preserve legacy POPMAPS behavior for
MaxEnt logistic suitability rasters.}

\item{resistance_epsilon}{Positive numeric scalar added to resistance values
before inversion to avoid infinite conductance when resistance is zero.}
}
\value{
A \code{popmaps_surface} object containing the original grid, optional
conductance raster, optional mask and barrier rasters, and transformation
metadata.
}
\description{
\code{prepare_popmaps_surface()} records the scientific meaning of a raster before
it is used for POPMAPS interpolation or surface comparison. Geographic
surfaces (\code{surface = "G"}) use only the raster geometry. Suitability- or
conductance-weighted surfaces (\code{surface = "C"}) use raster values to define
relative movement or gene-flow connectivity.

This helper creates a validated surface object for modern least-cost
interpolation, tuning, and candidate-surface comparison workflows.
}
\examples{
geographic <- prepare_popmaps_surface(hija_raster, surface = "G")
suitability <- prepare_popmaps_surface(
  hija_raster,
  surface = "C",
  surface_values = "suitability"
)

}
